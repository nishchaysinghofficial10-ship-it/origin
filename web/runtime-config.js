"use strict";

// This file contains public deployment configuration only. Never place a beta
// token or provider credential in the website bundle.
window.ORIGIN_BETA = Object.freeze({
  apiBase: ""
});
